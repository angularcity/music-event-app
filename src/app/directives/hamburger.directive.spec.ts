import { HamburgerDirective } from './hamburger.directive';

describe('HamburgerDirective', () => {
  it('should create an instance', () => {
    const directive = new HamburgerDirective();
    expect(directive).toBeTruthy();
  });
});
